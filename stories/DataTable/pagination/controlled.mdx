import { Story, Canvas } from '@storybook/addon-docs';

# Controlled Pagination

To enable controlled pagination you can add the `paginationPage` property in addition to `paginationServer` and or `pagination`.

You'll also likely want to use the `onChangePage` callback to keep the `paginationPage` property in sync with the data table's internal state.

<Canvas>
	<Story id="pagination-controlled--controlled" />
</Canvas>
